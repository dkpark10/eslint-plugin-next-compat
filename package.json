{
  "name": "eslint-plugin-next-compat-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm -r run dev",
    "lint": "pnpm --filter example run lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "packageManager": "pnpm@10.6.2"
}
